package display; //// <-- IGNORARRRR

import core.Controller; // <-- LÍNEA AÑADIDA

public class DisplayController extends Controller {
    //... (tu código raíz sin cambios) ...
    public DisplayController(String tag) {
        super(tag);
    }
}